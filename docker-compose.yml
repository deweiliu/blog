version: "3.3"

services:
  wordpress:
    image: deweiliu/blog:latest
    build:
      context: .
      dockerfile: Dockerfile    
    ports:
      - "80:80"
    #restart: always
    container_name: blog-container

