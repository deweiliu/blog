version: "3.3"

services:
  wordpress:
    image: deweiliu/wordpress:latest
    build:
      context: .
      dockerfile: Dockerfile    
    ports:
      - "80:80"
    restart: always
    container_name: wordpress-container

